{
  "name": "Some Name",
  "number": 123,
  "users{{repeatChild(100)}}": [{
    "firstName": "{{firstName}}",
    "lastName": "{{lastName}}",
    "email": "{{email}}",
    "dob": "{{date}}",
    "id": "{{guid}}"
  }]
}

stamplet.addGenerator('firstName', function(options){

});
